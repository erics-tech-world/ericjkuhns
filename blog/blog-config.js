// Your Blogger blog address (no https://, no trailing slash).
var BLOG_ID = "ericjkuhns.blogspot.com";

// Optional: filter this page to a single Blogger label (category).
// Leave as "" (empty) to show ALL posts.
// Examples: "tech", "skate", "client"
// Each themed page can set its own filter by loading a different config,
// or by setting window.BLOG_FILTER before this script — see themed pages.
var BLOG_FILTER = (typeof window.BLOG_FILTER !== "undefined") ? window.BLOG_FILTER : "";
